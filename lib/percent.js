const dare_normal_questions = `1%,2%,3%.split(','),`,
  functionpercent(){
  const all_questions = [dare_normal_questions], question_pack = all_questions[Math.floor(Math.random() * all_questions.length)], question = qu_pack[Math.floor(Math.random() * quespack.le)]; 
  return question;
}

    
module.exports = {
  percent,
}
